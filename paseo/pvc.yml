apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paseo-home
  namespace: paseo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
