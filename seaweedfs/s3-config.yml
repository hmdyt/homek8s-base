apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-config
  namespace: seaweedfs
type: Opaque
stringData:
  s3.json: |
    {
      "identities": [
        {
          "name": "admin",
          "credentials": [
            {
              "accessKey": "dummy",
              "secretKey": "dummy"
            }
          ],
          "actions": ["Admin", "Read", "Write"]
        }
      ]
    }
